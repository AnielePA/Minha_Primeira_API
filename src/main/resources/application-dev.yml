spring:
  datasource:
    url: jdbc:h2:mem:apifilmes
    username: aniele
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create # validate | update | create | create-drop
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      nabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false